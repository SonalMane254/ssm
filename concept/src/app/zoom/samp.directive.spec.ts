import { SampDirective } from './samp.directive';

describe('SampDirective', () => {
  it('should create an instance', () => {
    const directive = new SampDirective();
    expect(directive).toBeTruthy();
  });
});
